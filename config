theme = cyberdream

background-opacity = 0.7
background-blur-radius = 60

window-padding-x = 20
window-padding-balance = true

window-height = 50
window-width = 200

# Enable saving and restoring window state
# Make sure the setting:
# Desktop & Dock > Windows > Close windows when quitting an application
# is turned off
window-save-state = always

macos-option-as-alt = true
